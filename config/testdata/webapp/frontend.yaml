apiVersion: kustomize.fluxcd.io/v1alpha1
kind: Kustomization
metadata:
  name: frontend
spec:
  dependsOn:
    - common
    - backend
  interval: 5m
  path: "./webapp/frontend/"
  prune: "part-of=webapp,component=frontend"
  sourceRef:
    kind: GitRepository
    name: webapp
